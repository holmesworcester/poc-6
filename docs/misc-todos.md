- get a DAG-only test that tests the important parts of the DAG bootstrap: 
-- single player, link a device, invite people
-- and then synthetically add another user's events from the joining process 


- prove i have separated sync from connections
- connections should offer queues to sync and sync should pull from those queues. 
- try to make one connection per and

- consider having `pubkey` and `privkey` events and use them for invites, connections, invite proof, user signing of invites, etc. 