// E2E support file

// Add custom commands here
declare global {
  namespace Cypress {
    interface Chainable {
      // Add custom commands here if needed
    }
  }
}

// Prevent TypeScript from erroring on this file
export {}
